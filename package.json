{
  "name": "express-template",
  "version": "1.0.0",
  "description": "A template for developing web server using express.",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production npm run serve",
    "dev": "NODE_ENV=development npm run serve",
    "serve": "babel --plugins transform-node-env-inline src -d dist && node dist/index.js",
    "build": "npm run clean && NODE_ENV=production babel --plugins transform-node-env-inline src -d dist",
    "clean": "rm -rf dist"
  },
  "author": "Hyperbola",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "lodash": "^4.17.19",
    "mariadb": "^2.4.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.10.0",
    "@babel/core": "^7.10.0",
    "@babel/node": "^7.10.0",
    "@babel/preset-env": "^7.10.0",
    "babel-plugin-transform-node-env-inline": "^0.4.3"
  }
}
